{
  "name": "simple_todolist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir public",
    "serve": "lite-server --baseDir public --port 3000 --open --",
    "dev": "babel src --out-dir public --watch",
    "start": "npm run dev && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.20.7",
    "@babel/core": "^7.20.7",
    "@babel/preset-react": "^7.18.6",
    "lite-server": "^2.6.1",
    "yarn": "^1.22.19"
  }
}
